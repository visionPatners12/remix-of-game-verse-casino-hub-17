// Base hooks barrel export - Essential hooks only

export { usePersistedState } from './usePersistedState';

export type { 
  PersistedState, 
  PersistedActions, 
  UsePersistedStateOptions, 
  StorageType 
} from './usePersistedState';