// LI.FI SDK Configuration
// Re-exports from centralized chain config for backwards compatibility
export {
  SUPPORTED_CHAINS,
  SUPPORTED_CHAIN_IDS,
  DEFAULT_CHAIN_ID,
  CHAIN_ICONS,
  getChainName,
  getChainById as getChain,
} from './chains';
